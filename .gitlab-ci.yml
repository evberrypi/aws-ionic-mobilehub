image: marcoturi/ionic:latest

stages:
  - test

before_script:
  - yarn
  - npm run post-install

cache:
  untracked: true
  key: "$CI_PROJECT_ID"
  paths:
    - node_modules/
    - .yarn

build_web:
  stage: test
  only:
      - master
  script:
    - npm run test
    - npm run build
    - npm run e2e
